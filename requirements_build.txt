# Build dependencies for Talinda POS
# These are only needed for creating the installer, not for running the application

# Core build tools
cx_Freeze>=6.15.0
pyinstaller>=5.13.0
pyinstaller-hooks-contrib>=2023.8

# Alternative build tools
auto-py-to-exe>=2.36.0

# Development tools (optional)
wheel>=0.41.0
setuptools>=68.0.0 